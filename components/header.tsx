"use client";
import { Menu, Phone, X } from "lucide-react";
import { useState } from "react";
import { Button } from "./ui/button";
const links = [["Головна", "#home"], ["Міжкімнатні двері", "#catalog"], ["Вхідні двері", "#catalog"], ["Вікна", "#catalog"], ["Портфоліо", "#portfolio"], ["Контакти", "#contacts"]];
export function Header({ onMeasure }: { onMeasure: () => void }) { const [open, setOpen] = useState(false); return <header className="sticky top-0 z-40 border-b border-stone-200/80 bg-[#FCFBF8]/95 backdrop-blur"><div className="container-page flex h-20 items-center justify-between gap-4"><a href="#home" className="leading-none"><span className="font-display text-2xl tracking-tight">НАШІ</span><span className="ml-1.5 rounded-sm bg-clay px-1.5 py-0.5 text-xs font-bold tracking-[.16em] text-white">ДВЕРІ</span><small className="mt-1 block text-[9px] font-bold tracking-[.18em] text-stone-500">УЖГОРОД</small></a><nav className="hidden items-center gap-5 xl:flex">{links.map(([label, href]) => <a key={label} href={href} className="text-sm font-medium text-stone-600 transition hover:text-clay">{label}</a>)}</nav><div className="hidden items-center gap-3 lg:flex"><a href="tel:+380950729341" className="inline-flex items-center gap-2 text-sm font-bold"><Phone size={15} className="text-clay" />+38 (095) 072-93-41</a><Button onClick={onMeasure}>Замовити замір</Button></div><button aria-label="Відкрити меню" className="lg:hidden" onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button></div>{open && <div className="container-page border-t bg-[#FCFBF8] py-5 lg:hidden"><nav className="flex flex-col gap-4">{links.map(([label, href]) => <a onClick={() => setOpen(false)} key={label} href={href} className="text-sm font-medium">{label}</a>)}<Button className="w-full" onClick={() => { setOpen(false); onMeasure(); }}>Замовити замір</Button></nav></div>}</header> }
