"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { ClipboardCheck, Download, ExternalLink, FileUp, LoaderCircle, PackagePlus, RefreshCw, ShieldAlert } from "lucide-react";
import { AdminRouteGuard } from "@/components/admin-route-guard";
import { getSupabaseBrowserClient } from "@/lib/supabase-browser";

type Item = { url: string; title: string; image: string | null; brand: string; category: "interior" | "entrance"; collection: string };
type Preview = Item & { sourceUrl: string; productCode: string | null; description: string; images: string[]; facts: Array<{ label: string; value: string }> };
type Product = { slug: string; name: string; brand: string; collection: string; category: "interior" | "entrance" | "windows" };
type CatalogBrand = { id: string; name: string };
type CatalogCollection = { brand_id: string; name: string; category: "interior" | "entrance" | "windows" };
type SyncStatus = "same" | "similar" | "new";
type SyncRow = { item: Item; match: Product | null; status: SyncStatus };
const button = "inline-flex items-center justify-center gap-2 rounded-xl border border-stone-300 bg-white px-3 py-2.5 text-sm font-bold transition hover:border-clay disabled:cursor-not-allowed disabled:opacity-50";
function normalize(value: string) { return value.toLocaleLowerCase("uk-UA").replace(/(?:rodos|міжкімнатн[а-яіїє]*|межкомнатн[а-яіїє]*|двер[іь]|door)/giu, " ").replace(/[^\p{L}\p{N}]+/gu, " ").trim(); }
function looksSame(left: string, right: string) { const a = normalize(left); const b = normalize(right); return a.length > 4 && b.length > 4 && (a === b || a.includes(b) || b.includes(a)); }
function syncRow(item: Item, products: Product[]): SyncRow {
  const candidates = products.filter((product) => product.brand === item.brand && product.category === item.category);
  const exact = candidates.find((product) => normalize(product.name) === normalize(item.title));
  if (exact) return { item, match: exact, status: "same" };
  const similar = candidates.find((product) => looksSame(product.name, item.title));
  return similar ? { item, match: similar, status: "similar" } : { item, match: null, status: "new" };
}
function hash(value: string) { let result = 5381; for (const char of value) result = (result * 33) ^ char.charCodeAt(0); return (result >>> 0).toString(36); }
function decodeXml(value: string) { return value.replace(/&amp;/gi, "&").replace(/&quot;/gi, '"').replace(/<[^>]+>/g, "").trim(); }
function collectionFrom(value: string) { const text = value.toLocaleLowerCase("uk-UA"); if (/grand[- ]?delux|гранд.*делюкс/.test(text)) return "DELUX"; if (/grand[- ]?lux|гранд.*люкс/.test(text)) return "LUX"; if (/grand[- ]?paint|гранд.*пейнт/.test(text)) return "Paint"; if (/atlantic/.test(text)) return "Atlantic ПВХ"; if (/cortes/.test(text)) return "Cortes фарба"; if (/siena/.test(text)) return "Siena фарба"; if (/royal/.test(text)) return "Royal шпон"; if (/loft/.test(text) && /шпон|shpon/.test(text)) return "Loft шпон"; if (/loft/.test(text)) return "Loft фарба"; if (/style/.test(text)) return "Style ПВХ"; if (/woodmix/.test(text)) return "Woodmix"; if (/modern/.test(text)) return "Modern"; if (/gamma/.test(text)) return "Gamma"; if (/fresca/.test(text)) return "Fresca"; if (/liberta/.test(text)) return "Liberta"; return "Новинки Rodos"; }
function parseSitemap(xml: string): Item[] { const seen = new Set<string>(); const skipped = /(?:furnitur|фурнітур|plintus|плінтус|ustanov|монтаж|dostav|достав|contact|контакт|news|новин|akci|акці|review|відгук|faq|vopros|питан)/iu; return Array.from(xml.matchAll(/<url>([\s\S]*?)<\/url>/gi)).flatMap((match) => { const block = match[1]; const url = decodeXml(block.match(/<loc>([\s\S]*?)<\/loc>/i)?.[1] || ""); const title = decodeXml(block.match(/<image:title>([\s\S]*?)<\/image:title>/i)?.[1] || block.match(/<image:caption>([\s\S]*?)<\/image:caption>/i)?.[1] || ""); const image = decodeXml(block.match(/<image:loc>([\s\S]*?)<\/image:loc>/i)?.[1] || ""); const source = url + " " + title; if (!url || !title || skipped.test(source) || !/(?:двер|dver|door)/iu.test(source) || seen.has(url)) return []; seen.add(url); const entrance = /вхідн|vhodn|входн|rodos steel/iu.test(source); return [{ url, title, image: image || null, brand: entrance ? "Rodos Steel" : "Rodos", category: entrance ? "entrance" : "interior", collection: entrance ? (/для.*вулиц|dlya-ulicy|вулиц/iu.test(source) ? "Вулиця" : "Квартира") : collectionFrom(source) }]; }); }
function RodosImporterContent() {
  const supabase = useMemo(() => getSupabaseBrowserClient(), []);
  const [token, setToken] = useState(""); const [products, setProducts] = useState<Product[]>([]); const [brands, setBrands] = useState<CatalogBrand[]>([]); const [collections, setCollections] = useState<CatalogCollection[]>([]); const [items, setItems] = useState<Item[]>([]); const [active, setActive] = useState<Item | null>(null); const [preview, setPreview] = useState<Preview | null>(null); const [busy, setBusy] = useState(false); const [notice, setNotice] = useState(""); const [syncFilter, setSyncFilter] = useState<"all" | SyncStatus>("all"); const [backupAcknowledged, setBackupAcknowledged] = useState(false); const [replacementConfirm, setReplacementConfirm] = useState("");
  async function loadCurrentCatalog() {
    const [productResult, brandResult, collectionResult] = await Promise.all([
      supabase.from("products").select("slug,name,brand,collection,category").in("brand", ["Rodos", "Rodos Steel"]),
      supabase.from("catalog_brands").select("id,name").in("name", ["Rodos", "Rodos Steel"]),
      supabase.from("catalog_collections").select("brand_id,name,category"),
    ]);
    if (!productResult.error) setProducts((productResult.data || []) as Product[]);
    if (!brandResult.error) setBrands((brandResult.data || []) as CatalogBrand[]);
    if (!collectionResult.error) setCollections((collectionResult.data || []) as CatalogCollection[]);
  }
  useEffect(() => { supabase.auth.getSession().then(({ data }) => setToken(data.session?.access_token || "")); void loadCurrentCatalog(); const xml = sessionStorage.getItem("rodos-sitemap-xml"); if (xml) { const parsed = parseSitemap(xml); sessionStorage.removeItem("rodos-sitemap-xml"); if (parsed.length) { setItems(parsed); setNotice(`Завантажено з бібліотеки: ${parsed.length} карток.`); } } }, [supabase]);
  async function scan() { if (!token) return; setBusy(true); setNotice(""); try { const response = await fetch("/api/admin/import/rodos/catalog", { headers: { Authorization: "Bearer " + token } }); const data = await response.json(); if (!response.ok) throw new Error(data.message); setItems(data.products || []); } catch (error) { setNotice(error instanceof Error ? error.message : "Не вдалося зчитати sitemap."); } finally { setBusy(false); } }
  async function loadSitemapFile(file: File) { try { const parsed = parseSitemap(await file.text()); if (!parsed.length) throw new Error("У цьому файлі не знайдено карток дверей Rodos."); setItems(parsed); setActive(null); setPreview(null); setNotice(`Sitemap завантажено з файлу: ${parsed.length} карток.`); } catch (error) { setNotice(error instanceof Error ? error.message : "Не вдалося прочитати файл sitemap."); } }
  function downloadBackup() {
    const file = new Blob([JSON.stringify({ created_at: new Date().toISOString(), purpose: "Резервна копія перед заміною каталогу Rodos / Rodos Steel", products }, null, 2)], { type: "application/json" });
    const url = URL.createObjectURL(file); const link = document.createElement("a"); link.href = url; link.download = `rodos-before-replacement-${new Date().toISOString().slice(0, 10)}.json`; link.click(); URL.revokeObjectURL(url); setBackupAcknowledged(true); setNotice(`Резервну копію ${products.length} поточних моделей завантажено. Тепер можна запускати заміну.`);
  }
  async function ensureStructure() {
    const sourceBrands = ["Rodos", "Rodos Steel"];
    const missingBrands = sourceBrands.filter((name) => !brands.some((brand) => brand.name === name));
    if (missingBrands.length) {
      const result = await supabase.from("catalog_brands").upsert(missingBrands.map((name, index) => ({ name, description: name === "Rodos Steel" ? "Вхідні двері Rodos Steel" : "Міжкімнатні двері Rodos", is_active: true, sort_order: 50 + index })), { onConflict: "name" });
      if (result.error) throw new Error("Не вдалося створити фабрики: " + result.error.message);
    }
    const { data: currentBrands, error: currentBrandsError } = await supabase.from("catalog_brands").select("id,name").in("name", sourceBrands);
    if (currentBrandsError) throw new Error("Не вдалося прочитати фабрики: " + currentBrandsError.message);
    const brandMap = new Map((currentBrands || []).map((brand) => [brand.name, brand.id]));
    const neededCollections = Array.from(new Map(items.map((item) => [`${item.brand}:${item.category}:${item.collection}`, item])).values());
    const missingCollections = neededCollections.filter((item) => {
      const brandId = brandMap.get(item.brand);
      return brandId && !collections.some((collection) => collection.brand_id === brandId && collection.category === item.category && collection.name === item.collection);
    });
    if (missingCollections.length) {
      const result = await supabase.from("catalog_collections").upsert(missingCollections.map((item, index) => ({ brand_id: brandMap.get(item.brand), name: item.collection, category: item.category, description: `Колекція ${item.collection} фабрики ${item.brand}.`, is_active: true, sort_order: 100 + index })), { onConflict: "brand_id,name,category" });
      if (result.error) throw new Error("Не вдалося створити колекції: " + result.error.message);
    }
  }
  async function replaceCatalog() {
    if (!items.length) return setNotice("Спершу завантажте sitemap Rodos з картками дверей.");
    if (!backupAcknowledged) return setNotice("Спершу завантажте резервну копію поточного каталогу.");
    if (replacementConfirm.trim() !== "ЗАМІНИТИ RODOS") return setNotice("Для запуску введіть точно: ЗАМІНИТИ RODOS");
    if (!window.confirm(`Замінимо ${products.length} поточних моделей Rodos / Rodos Steel на ${items.length} прихованих чернеток з офіційного sitemap. Продовжити?`)) return;
    setBusy(true); setNotice("");
    try {
      await ensureStructure();
      const officialSlugs = new Set(items.map((item) => "rodos-official-" + hash(item.url)));
      const oldSlugs = products.map((product) => product.slug).filter((slug) => !officialSlugs.has(slug));
      const payload = items.map((item, index) => ({ slug: "rodos-official-" + hash(item.url), category: item.category, brand: item.brand, collection: item.collection, name: item.title, material: item.category === "entrance" ? "Вхідні" : "Міжкімнатні", style: "Колекція " + item.collection, color: "Уточнюється", price: "Ціна за запитом", description: `${item.title} — двері фабрики ${item.brand}, колекція ${item.collection}. Детальні характеристики та доступні комплектації уточнюйте у менеджера.`, features: [`Фабрика ${item.brand}`, `Колекція ${item.collection}`], image_path: item.image || "", sort_order: 90000 + index, is_available: false }));
      for (let offset = 0; offset < payload.length; offset += 40) {
        const result = await supabase.from("products").upsert(payload.slice(offset, offset + 40), { onConflict: "slug" });
        if (result.error) throw new Error("Не вдалося зберегти моделі: " + result.error.message);
      }
      const sources = items.map((item) => ({ product_slug: "rodos-official-" + hash(item.url), source_name: "Rodos", source_url: item.url, source_product_name: item.title, verification_status: "verified", verified_at: new Date().toISOString(), notes: "Імпортовано з офіційного sitemap rodos.ua. Чернетка потребує перевірки фото, опису та характеристик перед публікацією." }));
      for (let offset = 0; offset < sources.length; offset += 40) {
        const result = await supabase.from("product_sources").upsert(sources.slice(offset, offset + 40), { onConflict: "product_slug,source_url" });
        if (result.error) throw new Error("Моделі створено, але не вдалося зберегти частину джерел: " + result.error.message);
      }
      if (oldSlugs.length) {
        const result = await supabase.from("products").delete().in("slug", oldSlugs);
        if (result.error) throw new Error("Нові чернетки створено, але старі моделі не видалено: " + result.error.message);
      }
      setReplacementConfirm(""); setBackupAcknowledged(false); await loadCurrentCatalog(); setNotice(`Готово: створено або оновлено ${items.length} прихованих моделей. Старі моделі Rodos / Rodos Steel прибрано. Перевіряйте чернетки перед публікацією.`);
    } catch (error) { setNotice(error instanceof Error ? error.message : "Не вдалося завершити заміну каталогу."); } finally { setBusy(false); }
  }
  async function inspect(item: Item) { if (!token) return; setBusy(true); setActive(item); setPreview(null); setNotice(""); try { const response = await fetch("/api/admin/import/rodos?url=" + encodeURIComponent(item.url), { headers: { Authorization: "Bearer " + token } }); const data = await response.json(); if (!response.ok) throw new Error(data.message); setPreview(data); } catch (error) { setNotice(error instanceof Error ? error.message : "Не вдалося відкрити картку."); } finally { setBusy(false); } }
  async function addDraft() {
    if (!preview) return; const slug = "rodos-" + (preview.productCode || hash(preview.sourceUrl));
    if (products.some((product) => product.slug === slug)) { setNotice("Ця модель уже є в каталозі."); return; }
    setBusy(true); setNotice("");
    try {
      const image = preview.images[0] || preview.image;
      const inserted = await supabase.from("products").insert({ slug, category: preview.category, brand: preview.brand, collection: preview.collection, name: preview.title, material: preview.category === "entrance" ? "Вхідні" : "Міжкімнатні", style: "Колекція " + preview.collection, color: "Уточнюється", price: "Ціна за запитом", description: preview.description, features: [`Фабрика ${preview.brand}`, `Колекція ${preview.collection}`], image_path: image || null, sort_order: 99999, is_available: false });
      if (inserted.error) throw new Error(inserted.error.message);
      const requests: PromiseLike<{ error: { message: string } | null }>[] = [supabase.from("product_sources").insert({ product_slug: slug, source_name: "Rodos", source_url: preview.sourceUrl, source_product_name: preview.title, verification_status: "verified", verified_at: new Date().toISOString(), notes: "Імпорт з офіційної картки rodos.ua. Чернетка потребує перевірки перед публікацією." })];
      if (preview.facts.length) requests.push(supabase.from("product_specs").insert(preview.facts.map((fact, index) => ({ product_slug: slug, label: fact.label, value: fact.value, sort_order: 100 + index * 10, is_active: true }))));
      if (preview.images.length) requests.push(supabase.from("product_media").insert(preview.images.map((image, index) => ({ product_slug: slug, kind: index ? "gallery" : "main", label: index ? "Фото " + (index + 1) : "Головне фото", image_path: image, sort_order: index }))));
      const results = await Promise.all(requests); const failed = results.find((result) => result.error)?.error;
      setProducts((current) => [...current, { slug, name: preview.title, brand: preview.brand, collection: preview.collection, category: preview.category }]); setNotice(failed ? "Чернетку створено, але частина даних не збереглась: " + failed.message : "Чернетку створено. Вона прихована від покупців — перевірте й увімкніть її в «Товарах»." );
    } catch (error) { setNotice(error instanceof Error ? error.message : "Не вдалося додати чернетку."); } finally { setBusy(false); }
  }
  async function addBasicDraft() {
    if (!active) return; const slug = "rodos-" + hash(active.url); if (products.some((product) => product.slug === slug)) { setNotice("Ця модель уже є в каталозі."); return; }
    setBusy(true); try { const result = await supabase.from("products").insert({ slug, category: active.category, brand: active.brand, collection: active.collection, name: active.title, material: active.category === "entrance" ? "Вхідні" : "Міжкімнатні", style: "Колекція " + active.collection, color: "Уточнюється", price: "Ціна за запитом", description: `${active.title} — модель фабрики ${active.brand}. Повний опис і характеристики буде додано після перевірки офіційної картки.`, features: [`Фабрика ${active.brand}`, `Колекція ${active.collection}`], image_path: active.image, sort_order: 99999, is_available: false }); if (result.error) throw new Error(result.error.message); const source = await supabase.from("product_sources").insert({ product_slug: slug, source_name: "Rodos", source_url: active.url, source_product_name: active.title, verification_status: "verified", verified_at: new Date().toISOString(), notes: "Створено з офіційного sitemap Rodos. Потрібно доповнити дані картки." }); if (source.error) throw new Error(source.error.message); setProducts((current) => [...current, { slug, name: active.title, brand: active.brand, collection: active.collection, category: active.category }]); setNotice("Базову чернетку з фото sitemap додано. Вона прихована від покупців."); } catch (error) { setNotice(error instanceof Error ? error.message : "Не вдалося додати чернетку."); } finally { setBusy(false); }
  }
  const rows = useMemo(() => items.map((item) => syncRow(item, products)), [items, products]);
  const counts = useMemo(() => ({ new: rows.filter((row) => row.status === "new").length, same: rows.filter((row) => row.status === "same").length, similar: rows.filter((row) => row.status === "similar").length, collections: new Set(rows.map((row) => `${row.item.brand}:${row.item.collection}`)).size }), [rows]);
  const filteredRows = syncFilter === "all" ? rows : rows.filter((row) => row.status === syncFilter);
  const statusCopy: Record<SyncStatus, { title: string; tone: string; detail: string }> = { same: { title: "Вже є", tone: "bg-green-100 text-green-800", detail: "Оновлюватимемо існуючу картку, без дубля." }, similar: { title: "Потрібно звірити", tone: "bg-amber-100 text-amber-800", detail: "Може бути ця сама модель під іншою назвою." }, new: { title: "Нова", tone: "bg-sky-100 text-sky-800", detail: "Створимо приховану чернетку у відповідній колекції." } };
  return <section className="space-y-5"><div className="rounded-3xl bg-ink px-5 py-8 text-white sm:px-8"><p className="text-xs font-bold uppercase tracking-[.16em] text-sand">Офіційний імпорт</p><h1 className="mt-2 font-display text-4xl">Каталог Rodos</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/70">Сканер читає sitemap rodos.ua. Модель потрапляє у каталог лише як прихована чернетка, тому покупці не побачать її до вашої перевірки.</p><div className="mt-5 flex flex-wrap gap-2"><button className={button + " border-white/20 bg-white text-ink"} onClick={scan} disabled={busy || !token}>{busy ? <><LoaderCircle className="animate-spin" size={16} /> Скануємо…</> : <><RefreshCw size={16} /> Сканувати sitemap</>}</button><a className={button + " border-white/30 bg-transparent text-white hover:bg-white/10"} href="https://rodos.ua/sitemap.xml" target="_blank" rel="noreferrer">Відкрити sitemap <ExternalLink size={15} /></a><label className={button + " cursor-pointer border-white/30 bg-transparent text-white hover:bg-white/10"}><FileUp size={15} /> Завантажити sitemap-файл<input className="hidden" type="file" accept=".xml,text/xml,application/xml" onChange={(event) => { const file = event.target.files?.[0]; if (file) void loadSitemapFile(file); event.currentTarget.value = ""; }} /></label></div><p className="mt-3 text-xs leading-5 text-white/60">Якщо Rodos обмежив серверне сканування, відкрий sitemap, збережи XML-файл і завантаж його сюди.</p></div>
    {notice && <p role="status" className="rounded-xl border bg-white p-4 text-sm text-stone-700">{notice}</p>}
    {items.length > 0 && <section className="rounded-2xl border border-red-200 bg-red-50 p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.14em] text-red-800"><ShieldAlert size={16} /> Повна заміна каталогу</p><h2 className="mt-2 font-display text-3xl text-ink">Очистити й заново імпортувати Rodos</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-stone-700">Створимо <b>{items.length} прихованих моделей</b> з офіційного sitemap, автоматично додамо відсутні колекції Rodos та Rodos Steel, а потім приберемо старі товари цих двох фабрик. На сайт нічого не потрапить до ручного увімкнення моделей.</p></div><button type="button" onClick={downloadBackup} disabled={busy || !products.length} className={button + " border-red-200 bg-white text-red-800 hover:border-red-400"}><Download size={16} /> {backupAcknowledged ? "Резервну копію збережено" : `Завантажити backup (${products.length})`}</button></div><div className="mt-5 grid gap-3 lg:grid-cols-[minmax(0,1fr)_260px]"><label className="text-sm font-bold text-stone-800">Введіть <span className="rounded bg-white px-1.5 py-0.5 font-mono text-xs">ЗАМІНИТИ RODOS</span>, щоб активувати кнопку<input className="mt-2 w-full rounded-xl border border-red-200 bg-white px-3 py-2.5 text-sm outline-none focus:border-red-500 focus:ring-2 focus:ring-red-200" value={replacementConfirm} onChange={(event) => setReplacementConfirm(event.target.value)} placeholder="ЗАМІНИТИ RODOS" /></label><button type="button" disabled={busy || !backupAcknowledged || replacementConfirm.trim() !== "ЗАМІНИТИ RODOS"} onClick={replaceCatalog} className="inline-flex items-center justify-center gap-2 self-end rounded-xl bg-red-700 px-4 py-3 text-sm font-bold text-white transition hover:bg-red-800 disabled:cursor-not-allowed disabled:opacity-50"><ShieldAlert size={16} /> {busy ? "Виконуємо…" : "Замінити каталог Rodos"}</button></div><p className="mt-3 text-xs leading-5 text-red-800">Backup містить перелік поточних товарів для звірки. Самі фото у Storage не видаляються, а записи товарів, декорів, характеристик і джерел старих моделей буде видалено каскадно.</p></section>}
    {items.length > 0 && <section className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-clay">План синхронізації — без змін у каталозі</p><h2 className="mt-1 font-display text-3xl">Знайдено {items.length} карток</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-stone-600">Спершу звіряємо новий каталог виробника з вашим. Лише після підтвердження моделі будуть оновлені або додані як приховані чернетки.</p></div><Link href="/admin/catalog?brand=Rodos&quality=hidden" className="button-light">Чернетки Rodos</Link></div><div className="mt-5 grid gap-2 sm:grid-cols-2 xl:grid-cols-4">{([['new', 'Нові моделі', counts.new, 'bg-sky-50 text-sky-900'], ['same', 'Уже є в каталозі', counts.same, 'bg-green-50 text-green-900'], ['similar', 'Треба звірити', counts.similar, 'bg-amber-50 text-amber-900'], ['all', 'Колекцій знайдено', counts.collections, 'bg-stone-100 text-ink']] as const).map(([id, label, count, color]) => <button key={id} type="button" onClick={() => setSyncFilter(id)} className={"rounded-xl p-3 text-left transition " + color + (syncFilter === id ? " ring-2 ring-ink" : " hover:ring-2 hover:ring-stone-300")}><span className="block text-2xl font-display">{count}</span><span className="mt-1 block text-xs font-bold">{label}</span></button>)}</div><div className="mt-5 flex flex-wrap items-center justify-between gap-3"><p className="inline-flex items-center gap-2 text-sm text-stone-600"><ClipboardCheck size={17} className="text-clay" /> Показано {filteredRows.length} моделей. Натисни модель, щоб перевірити її офіційну картку.</p><div className="flex flex-wrap gap-2 text-xs">{(["same", "similar", "new"] as SyncStatus[]).map((status) => <span key={status} className={"rounded-full px-2.5 py-1 font-bold " + statusCopy[status].tone}>{statusCopy[status].title}</span>)}</div></div><div className="mt-5 grid gap-2 lg:grid-cols-2">{filteredRows.map(({ item, match, status }) => <div key={item.url} className="rounded-xl border border-stone-200 p-3 transition hover:border-clay hover:bg-sand"><div className="flex items-start justify-between gap-3"><button onClick={() => inspect(item)} className="min-w-0 text-left"><b className="block text-sm">{item.title}</b><span className="mt-1 block text-xs text-stone-500">{item.brand} · {item.collection}</span></button><span className={"shrink-0 rounded-full px-2 py-1 text-xs font-bold " + statusCopy[status].tone}>{statusCopy[status].title}</span></div><p className="mt-2 text-xs leading-5 text-stone-500">{statusCopy[status].detail}</p>{match && <div className="mt-2 flex flex-wrap items-center gap-2"><span className="text-xs text-stone-600">У нас: <b>{match.name}</b> · {match.collection}</span><Link href={`/admin/catalog?product=${encodeURIComponent(match.slug)}`} className="text-xs font-bold text-clay underline">Відкрити нашу картку →</Link></div>}<button type="button" onClick={() => inspect(item)} className="mt-3 text-xs font-bold text-clay underline">Перевірити у виробника →</button></div>)}</div></section>}
    {active && <section className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-clay">Перевірка моделі</p><h2 className="mt-1 font-display text-3xl">{active.title}</h2></div><a className="button-light" href={active.url} target="_blank" rel="noreferrer">У виробника <ExternalLink size={15} /></a></div>{busy && !preview ? <p className="mt-5 text-sm text-stone-600">Завантажуємо фото, опис і характеристики…</p> : preview ? <><div className="mt-5 grid gap-5 lg:grid-cols-[260px_1fr]"><div>{preview.images[0] && <img src={preview.images[0]} alt={preview.title} className="h-72 w-full rounded-xl border object-contain p-2" />}</div><div><div className="flex flex-wrap gap-2 text-xs font-bold"><span className="rounded-full bg-sand px-2.5 py-1">{preview.brand}</span><span className="rounded-full bg-sand px-2.5 py-1">{preview.collection}</span><span className="rounded-full bg-stone-100 px-2.5 py-1">буде прихована</span></div><p className="mt-4 text-sm leading-6 text-stone-700">{preview.description}</p><dl className="mt-4 grid gap-x-6 text-sm sm:grid-cols-2">{preview.facts.slice(0, 12).map((fact) => <div key={fact.label} className="flex justify-between gap-3 border-b py-2"><dt className="text-stone-500">{fact.label}</dt><dd className="text-right font-semibold">{fact.value}</dd></div>)}</dl></div></div><div className="mt-5 flex flex-wrap gap-2 border-t pt-4"><button className="button-primary" onClick={addDraft} disabled={busy || products.some((product) => product.slug === "rodos-" + (preview.productCode || hash(preview.sourceUrl)))}><PackagePlus size={16} /> Додати як чернетку</button><Link className="button-light" href="/admin/catalog?brand=Rodos&quality=hidden">Перевірити чернетки</Link></div></> : <div className="mt-5 border-t pt-4"><p className="text-sm text-stone-600">Повну картку Rodos зараз не вдалося завантажити через обмеження фабрики. Можна безпечно додати базову приховану чернетку з назвою, фото та посиланням із sitemap.</p><button className="button-primary mt-3" onClick={addBasicDraft} disabled={busy || products.some((product) => product.slug === "rodos-" + hash(active.url))}><PackagePlus size={16} /> Додати базову чернетку</button></div>}</section>}
  </section>;
}
export function RodosImporter() { return <AdminRouteGuard><RodosImporterContent /></AdminRouteGuard>; }
