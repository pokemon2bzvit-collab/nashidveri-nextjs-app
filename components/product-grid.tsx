import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import type { Product } from "@/lib/catalog";
export function ProductGrid({ products }: { products: Product[] }) { return <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{products.map((product) => <article className="group overflow-hidden rounded-2xl border bg-white shadow-soft" key={product.slug}><Link href={`/catalog/${product.slug}`} className="block"><div className="aspect-[1.12] overflow-hidden"><img className="h-full w-full object-cover transition duration-500 group-hover:scale-105" src={product.image} alt={product.name} /></div><div className="p-5"><p className="text-lg font-bold">{product.name}</p><p className="mt-1 text-sm text-stone-500">{product.features.join(" · ")}</p><div className="mt-5 flex items-center justify-between"><span className="font-display text-xl">{product.price}</span><span className="rounded-full border p-2.5 transition group-hover:bg-ink group-hover:text-white"><ArrowUpRight size={18} /></span></div></div></Link></article>)}</div> }
