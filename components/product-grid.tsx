import Link from "next/link";
import { ArrowUpRight, Check } from "lucide-react";
import { categories, type Product } from "@/lib/catalog";

export function ProductGrid({ products }: { products: Product[] }) {
  return <div className="grid grid-cols-2 gap-3 sm:gap-5 lg:grid-cols-3">{products.map((product) => <article className="group surface-card overflow-hidden" key={product.slug}><Link href={`/catalog/${product.slug}`} className="block"><div className="relative aspect-[4/5] overflow-hidden bg-[#f7f5f1] p-2 sm:p-4"><img className="h-full w-full object-contain transition duration-500 group-hover:scale-[1.03]" src={product.image} alt={product.name} /><span className="absolute left-2 top-2 rounded-full bg-white px-2 py-1 text-[8px] font-bold uppercase tracking-wider text-ink shadow-sm sm:left-4 sm:top-4 sm:px-3 sm:py-1.5 sm:text-[10px]">{categories[product.category].short}</span><span className="absolute right-2 top-2 flex h-7 w-7 items-center justify-center rounded-full bg-ink/80 text-white backdrop-blur sm:right-4 sm:top-4 sm:h-8 sm:w-8"><ArrowUpRight size={14} /></span></div><div className="p-3 sm:p-5"><p className="text-[9px] font-bold uppercase tracking-[.1em] text-clay sm:text-[11px] sm:tracking-[.14em]">{product.style}</p><p className="mt-1 text-base font-bold leading-tight sm:text-xl">{product.name}</p><div className="mt-3 hidden flex-wrap gap-2 sm:flex">{product.features.slice(0, 2).map((feature) => <span key={feature} className="rounded-full bg-sand px-2.5 py-1 text-[11px] font-medium text-stone-600">{feature}</span>)}</div><div className="mt-3 flex items-end justify-between border-t pt-3 sm:mt-5 sm:pt-4"><div><p className="text-[8px] font-bold uppercase tracking-wider text-stone-400 sm:text-[10px]">Ціна</p><span className="mt-1 block font-display text-base leading-tight sm:text-2xl">{product.price}</span><p className="mt-1 hidden text-[11px] font-semibold text-pine sm:block">Наявність: уточнюйте</p></div><span className="hidden items-center gap-1 text-xs font-bold text-pine sm:flex"><Check size={15} /> Детальніше</span></div></div></Link></article>)}</div>;
}
