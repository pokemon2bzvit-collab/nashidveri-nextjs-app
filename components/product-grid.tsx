import Link from "next/link";
import { ArrowUpRight, Check } from "lucide-react";
import { categories, type Product } from "@/lib/catalog";
export function ProductGrid({ products }: { products: Product[] }) { return <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{products.map((product) => <article className="group surface-card overflow-hidden" key={product.slug}><Link href={`/catalog/${product.slug}`} className="block"><div className="relative aspect-[1.1] overflow-hidden bg-sand"><img className="h-full w-full object-cover transition duration-500 group-hover:scale-105" src={product.image} alt={product.name} /><span className="absolute left-4 top-4 rounded-full bg-white px-3 py-1.5 text-[10px] font-bold uppercase tracking-wider text-ink shadow-sm">{categories[product.category].short}</span><span className="absolute right-4 top-4 flex h-8 w-8 items-center justify-center rounded-full bg-ink/80 text-white backdrop-blur"><ArrowUpRight size={15} /></span></div><div className="p-5"><p className="text-[11px] font-bold uppercase tracking-[.14em] text-clay">{product.style}</p><p className="mt-1 text-xl font-bold">{product.name}</p><div className="mt-4 flex flex-wrap gap-2">{product.features.slice(0, 2).map((feature) => <span key={feature} className="rounded-full bg-sand px-2.5 py-1 text-[11px] font-medium text-stone-600">{feature}</span>)}</div><div className="mt-5 flex items-end justify-between border-t pt-4"><div><p className="text-[10px] font-bold uppercase tracking-wider text-stone-400">Орієнтовна ціна</p><span className="mt-1 block font-display text-2xl">{product.price}</span><p className="mt-1 text-[11px] font-semibold text-pine">Наявність: уточнюйте</p></div><span className="flex items-center gap-1 text-xs font-bold text-pine"><Check size={15} /> Детальніше</span></div></div></Link></article>)}</div> }
