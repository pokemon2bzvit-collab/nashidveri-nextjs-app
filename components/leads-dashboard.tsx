"use client";
import { AlertTriangle, ClipboardList, LoaderCircle, NotebookPen, Phone, RefreshCw, Search, Trash2 } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { getSupabaseBrowserClient } from "@/lib/supabase-browser";

type Priority = "normal" | "important" | "urgent";
type Lead = { id: string; created_at: string; name: string; phone: string; contact_method: string; message: string | null; product_name: string | null; request_type: string; status: string; priority: Priority; manager_note: string | null };
const statusLabels: Record<string, string> = { new: "Нова", in_progress: "У роботі", measurement: "Замір", offer_sent: "Прорахунок надіслано", won: "Успішно", lost: "Відмова" };
const typeLabels: Record<string, string> = { measurement: "Замір", price_request: "Прорахунок", contact_form: "Повідомлення", consultation: "Консультація" };
const priorityLabels: Record<Priority, string> = { normal: "Звичайна", important: "Важлива", urgent: "Термінова" };

export function LeadsDashboard() {
  const supabase = useMemo(() => getSupabaseBrowserClient(), []);
  const [leads, setLeads] = useState<Lead[]>([]); const [loading, setLoading] = useState(true); const [notice, setNotice] = useState(""); const [query, setQuery] = useState(""); const [statusFilter, setStatusFilter] = useState("all");
  async function load() { setLoading(true); const { data, error } = await supabase.from("leads").select("id,created_at,name,phone,contact_method,message,request_type,product_name,status,priority,manager_note").order("created_at", { ascending: false }).limit(100); setLoading(false); if (error) setNotice("Спершу виконайте SQL-файли leads-priority.sql та leads-manager-notes.sql у Supabase. " + error.message); else { setLeads((data || []) as Lead[]); setNotice(""); } }
  useEffect(() => { load(); }, []);
  async function updateLead(id: string, patch: Partial<Pick<Lead, "status" | "priority" | "manager_note">>) { const { error } = await supabase.from("leads").update({ ...patch, updated_at: new Date().toISOString() }).eq("id", id); if (error) setNotice(error.message); else setLeads((items) => items.map((item) => item.id === id ? { ...item, ...patch } : item)); }
  async function removeLead(lead: Lead) { if (!window.confirm(`Видалити заявку від «${lead.name}»? Цю дію не можна скасувати.`)) return; const { error } = await supabase.from("leads").delete().eq("id", lead.id); if (error) setNotice(error.message); else setLeads((items) => items.filter((item) => item.id !== lead.id)); }
  const filteredLeads = leads.filter((lead) => (statusFilter === "all" || lead.status === statusFilter) && `${lead.name} ${lead.phone} ${lead.product_name || ""} ${lead.message || ""}`.toLowerCase().includes(query.toLowerCase()));
  const newLeads = leads.filter((lead) => lead.status === "new").length; const activeLeads = leads.filter((lead) => ["new", "in_progress", "measurement", "offer_sent"].includes(lead.status)).length; const urgentLeads = leads.filter((lead) => lead.priority === "urgent").length;
  return <section className="mt-5 rounded-2xl border bg-white p-4 shadow-sm sm:p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-clay">Звернення клієнтів</p><h3 className="mt-1 flex items-center gap-2 font-display text-3xl"><ClipboardList size={24} /> Заявки</h3></div><button onClick={load} className="button-light px-3 py-2 text-sm"><RefreshCw size={15} /> Оновити</button></div>
    {loading ? <div className="grid min-h-32 place-items-center text-sm text-stone-500"><span className="inline-flex items-center gap-2"><LoaderCircle className="animate-spin" size={17} /> Завантаження…</span></div> : <><div className="mt-5 grid gap-3 sm:grid-cols-3"><button type="button" onClick={() => setStatusFilter("new")} className={"rounded-xl border p-3 text-left " + (statusFilter === "new" ? "border-clay bg-sand" : "border-stone-200 bg-stone-50")}><b className="block font-display text-2xl">{newLeads}</b><span className="text-xs text-stone-600">нові — передзвонити</span></button><button type="button" onClick={() => setStatusFilter("all")} className={"rounded-xl border p-3 text-left " + (statusFilter === "all" ? "border-clay bg-sand" : "border-stone-200 bg-stone-50")}><b className="block font-display text-2xl">{activeLeads}</b><span className="text-xs text-stone-600">активні в роботі</span></button><div className={"rounded-xl border p-3 " + (urgentLeads ? "border-red-200 bg-red-50" : "border-stone-200 bg-stone-50")}><b className="block font-display text-2xl">{urgentLeads}</b><span className="text-xs text-stone-600">термінові заявки</span></div></div>
      <div className="mt-4 grid gap-3 md:grid-cols-[minmax(0,1fr)_230px]"><label className="relative"><Search className="pointer-events-none absolute left-3 top-3 text-stone-400" size={17} /><input className="w-full rounded-xl border border-stone-300 bg-white py-2.5 pl-10 pr-3 text-sm outline-none focus:border-clay focus:ring-2 focus:ring-clay/20" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Пошук: ім’я, телефон, модель або повідомлення" /></label><select className="rounded-xl border border-stone-300 bg-white px-3 py-2.5 text-sm font-bold outline-none focus:border-clay" value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)}><option value="all">Усі статуси</option>{Object.entries(statusLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></div>
      <div className="mt-5 space-y-3">{filteredLeads.map((lead) => <article key={lead.id} className={"rounded-xl border p-4 " + (lead.priority === "urgent" ? "border-red-300 bg-red-50/50" : lead.priority === "important" ? "border-amber-300 bg-amber-50/50" : lead.status === "new" ? "border-clay/40 bg-sand/40" : "border-stone-200 bg-stone-50")}><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex flex-wrap items-center gap-2"><b>{lead.name}</b>{lead.priority !== "normal" && <span className={"inline-flex items-center gap-1 rounded-full px-2 py-1 text-[10px] font-bold " + (lead.priority === "urgent" ? "bg-red-100 text-red-800" : "bg-amber-100 text-amber-800")}><AlertTriangle size={12} /> {priorityLabels[lead.priority]}</span>}</div><p className="mt-1 flex items-center gap-1 text-sm text-stone-600"><Phone size={14} /> <a className="underline hover:text-clay" href={"tel:" + lead.phone}>{lead.phone}</a> · {lead.contact_method === "phone" ? "дзвінок" : lead.contact_method}</p></div><div className="flex items-center gap-2"><span className="text-xs text-stone-500">{new Intl.DateTimeFormat("uk-UA", { dateStyle: "short", timeStyle: "short" }).format(new Date(lead.created_at))}</span><button type="button" onClick={() => removeLead(lead)} className="rounded-lg p-2 text-stone-400 transition hover:bg-white hover:text-red-600" aria-label={`Видалити заявку від ${lead.name}`}><Trash2 size={16} /></button></div></div><p className="mt-3 text-sm"><b>{typeLabels[lead.request_type] || lead.request_type}</b>{lead.product_name ? " · " + lead.product_name : ""}</p>{lead.message && <p className="mt-2 whitespace-pre-line text-sm leading-6 text-stone-600">{lead.message}</p>}<label className="mt-3 block text-xs font-bold text-stone-600"><span className="flex items-center gap-1"><NotebookPen size={14} /> Нотатка менеджера</span><textarea defaultValue={lead.manager_note || ""} onBlur={(event) => { const note = event.target.value.trim(); if (note !== (lead.manager_note || "")) updateLead(lead.id, { manager_note: note || null }); }} rows={2} placeholder="Наприклад: передзвонити після 17:00, потрібен монтаж" className="mt-1 w-full max-w-xl resize-y rounded-lg border border-stone-300 bg-white px-2 py-2 text-sm font-normal outline-none focus:border-clay" /></label><div className="mt-3 grid max-w-lg gap-2 sm:grid-cols-2"><label className="text-xs font-bold text-stone-600">Етап роботи<select className="mt-1 w-full rounded-lg border border-stone-300 bg-white px-2 py-2 text-sm" value={lead.status} onChange={(event) => updateLead(lead.id, { status: event.target.value })}>{Object.entries(statusLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label className="text-xs font-bold text-stone-600">Пріоритет<select className="mt-1 w-full rounded-lg border border-stone-300 bg-white px-2 py-2 text-sm" value={lead.priority} onChange={(event) => updateLead(lead.id, { priority: event.target.value as Priority })}>{Object.entries(priorityLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label></div></article>)}{!filteredLeads.length && !notice && <p className="rounded-xl bg-sand p-4 text-sm text-stone-600">За цими умовами заявок немає.</p>}</div>
    </>}{notice && <p className="mt-4 rounded-xl bg-sand p-3 text-sm text-stone-700">{notice}</p>}</section>;
}
