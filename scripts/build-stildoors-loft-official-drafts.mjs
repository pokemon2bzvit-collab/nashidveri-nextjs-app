import { mkdirSync, writeFileSync } from 'node:fs';
const baseUrl='https://stildoors.com.ua',headers={'user-agent':'Mozilla/5.0 (compatible; NashiDveriCatalogAudit/1.0)'},outputSql='supabase/generated/stildoors-loft-official-drafts.sql',outputJson='supabase/generated/stildoors-loft-official-drafts.json';
const sleep=(ms)=>new Promise((resolve)=>setTimeout(resolve,ms));const sql=(value)=>`'${String(value??'').replaceAll("'","''")}'`;const nullable=(value)=>value==null?'null':sql(value);
const entries=[['Loft','loft','biliy-mat/gluhe'],['Loft Aluminium','loft-aluminium','biliy-mat/molding-chorniy-kromka-chorna'],['Loft Glass','loft-glass','biliy-mat/sklo-chorne'],['Loft Intro','loft-intro','biliy-mat/molding-chorniy'],['Loft Line','loft-line','biliy-mat/kromka-chorniy-mat'],['Loft Rain','loft-rain','biliy-mat/molding-chorniy']];
const text=(value)=>value.replace(/<[^>]*>/g,' ').replace(/&nbsp;/g,' ').replace(/&amp;/g,'&').replace(/\s+/g,' ').trim();function specsFrom(html){const result={};for(const row of html.matchAll(/<tr>\s*<td class=['"]specification-title['"]>([\s\S]*?)<\/td>\s*<td class=['"]specification-description['"]>([\s\S]*?)<\/td>\s*<\/tr>/gi)){const label=text(row[1]),value=text(row[2]);if(label&&value)result[label]=value;}return result;}
const labels=new Map([['Розміри','Розміри полотна'],['Товщина полотна','Товщина полотна'],['Доступні кольори','Декори']]),models=[];
for(const[name,modelSlug,path]of entries){const sourceUrl=`${baseUrl}/dveri/loft/${modelSlug}/${path}/`,response=await fetch(sourceUrl,{headers}),html=await response.text();if(!response.ok)throw new Error(`${name}: ${response.status}`);const relativeImage=html.match(/<meta\s+property=['"]og:image['"]\s+content=['"]([^'"]+)['"]/i)?.[1];if(!relativeImage)throw new Error(`${name}: не знайдено фото`);const sourceSpecs=specsFrom(html),colors=[...new Set(sourceSpecs['Доступні кольори']?.split(',').map(text).filter(Boolean)??[])],specs=Object.entries(sourceSpecs).filter(([label])=>labels.has(label)).map(([label,value],index)=>({label:labels.get(label),value:label==='Доступні кольори'?colors.join(', '):value,sortOrder:100+index*10})),dimensions=sourceSpecs['Розміри'],thickness=sourceSpecs['Товщина полотна'];models.push({slug:`stildoors-loft-${modelSlug}-official`,name:`StilDoors ${name}`,sourceUrl,imagePath:new URL(relativeImage,baseUrl).href,colors,specs,description:`StilDoors ${name} — міжкімнатні двері колекції LOFT. ${dimensions?`Доступні стандартні розміри: ${dimensions}. `:''}${thickness?`Товщина полотна ${thickness}. `:''}Для моделі передбачені заводські декори та варіанти оформлення; актуальну комплектацію й ціну уточнюйте у менеджера.`});await sleep(300);}
const products=models.map((item,index)=>`(${[item.slug,'interior','StilDoors','LOFT',item.name,'Міжкімнатні','Ламіновані двері','Варіанти заводських декорів','Ціна за запитом',item.description,JSON.stringify(['Фабрика StilDoors','Колекція LOFT','Офіційна картка виробника']),item.imagePath,10000+index,false].map(sql).join(', ')})`),specs=models.flatMap((item)=>item.specs.map((spec)=>`(${[item.slug,spec.label,spec.value,spec.sortOrder,true].map(sql).join(', ')})`)),media=models.map((item)=>`(${[item.slug,'main','Головне фото',item.imagePath,0,true].map(sql).join(', ')})`),options=models.flatMap((item)=>item.colors.map((color,index)=>`(${[sql(item.slug),sql('color'),sql('Колір полотна'),sql(color),nullable(null),nullable(null),sql(index+1),sql(true)].join(', ')})`)),sources=models.map((item)=>`(${[item.slug,'StilDoors',item.sourceUrl,item.name,'verified','now()','Офіційна картка StilDoors: назва, головне фото, розміри, товщина та доступні декори.'].map((value,index)=>index===5?value:sql(value)).join(', ')})`);
const lines=['-- StilDoors LOFT: 6 офіційних моделей як приховані чернетки.','begin;',"insert into public.catalog_collections (brand_id,name,category,description,is_active,sort_order) select id,'LOFT','interior','Міжкімнатні двері StilDoors колекції LOFT.',true,74 from public.catalog_brands where name='StilDoors' on conflict (brand_id,name,category) do update set description=excluded.description,is_active=true,updated_at=now();",'insert into public.products (slug,category,brand,collection,name,material,style,color,price,description,features,image_path,sort_order,is_available) values',`${products.join(',\n')}\non conflict (slug) do update set name=excluded.name,material=excluded.material,style=excluded.style,color=excluded.color,description=excluded.description,features=excluded.features,image_path=excluded.image_path,sort_order=excluded.sort_order,is_available=false,updated_at=now();`,'insert into public.product_specs (product_slug,label,value,sort_order,is_active) values',`${specs.join(',\n')}\non conflict (product_slug,label) do update set value=excluded.value,sort_order=excluded.sort_order,is_active=true;`,'insert into public.product_media (product_slug,kind,label,image_path,sort_order,is_active) values',`${media.join(',\n')}\non conflict (product_slug,kind,image_path) do update set label=excluded.label,sort_order=excluded.sort_order,is_active=true;`,'insert into public.product_options (product_slug,option_group,group_label,label,swatch,image_path,sort_order,is_active) values',`${options.join(',\n')}\non conflict (product_slug,option_group,label) do update set group_label=excluded.group_label,sort_order=excluded.sort_order,is_active=true;`,'insert into public.product_sources (product_slug,source_name,source_url,source_product_name,verification_status,verified_at,notes) values',`${sources.join(',\n')}\non conflict (product_slug,source_url) do update set source_product_name=excluded.source_product_name,verification_status='verified',verified_at=now(),notes=excluded.notes;`,'commit;',"select count(*) as офіційних_чернеток from public.products where brand='StilDoors' and collection='LOFT' and slug like 'stildoors-loft-%-official' and not is_available;"];
mkdirSync('supabase/generated',{recursive:true});writeFileSync(outputSql,`${lines.join('\n')}\n`);writeFileSync(outputJson,`${JSON.stringify(models,null,2)}\n`);console.log(`Створено ${outputSql}: ${models.length} прихованих моделей LOFT.`);
