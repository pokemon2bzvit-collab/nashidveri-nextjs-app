import { SitemapLibrary } from "@/components/sitemap-library";

export const metadata = { title: "Sitemap-файли — Адмінка «Наші двері»", robots: { index: false, follow: false } };
export default function SitemapsPage() { return <SitemapLibrary />; }
