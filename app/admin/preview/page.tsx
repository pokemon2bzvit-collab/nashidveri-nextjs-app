import { SitePreview } from "@/components/site-preview";

export const metadata = { title: "Перегляд сайту — Адмінка «Наші двері»", robots: { index: false, follow: false } };
export default function PreviewPage() { return <SitePreview />; }
