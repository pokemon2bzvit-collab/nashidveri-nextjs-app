import { ImporterHub } from "@/components/importer-hub";
export const metadata = { title: "Імпортери — Адмінка «Наші двері»", robots: { index: false, follow: false } };
export default function ImportersPage() { return <ImporterHub />; }
