import { AdminQuality } from "@/components/admin-quality";

export const metadata = { title: "Якість каталогу — Адмінка", robots: { index: false, follow: false } };
export default function QualityPage() { return <AdminQuality />; }
